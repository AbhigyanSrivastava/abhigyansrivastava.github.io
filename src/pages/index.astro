---
import PostPreview from "@/components/blog/PostPreview.astro";
import PageLayout from "@/layouts/Base.astro";
import { getAllPosts, sortMDByDate } from "@/utils";
import rocket from '../assets/train.gif';
const MAX_POSTS = 10;
const allPosts = await getAllPosts();
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);

const cactusTech: { desc: string; href: string; title: string }[] = [];
---
<script defer src="https://cloud.umami.is/script.js" data-website-id="d8126afa-dc93-427c-a836-8e92a8586a7d"></script>
<PageLayout meta={{ title: "Home" }}>
	<section>
		<p class="mb-5">
   <h1 class="title mb-7" style="display: flex; justify-content: center; align-items: center; height: 100vh;">Namaste🙏</h1>
      <br>
      I am Abhigyan and this website is my mind rambling in kilobytes.<br>
      Expect half-baked thoughts, doodles, full-fledged posts or anything in between.<br>
      <br>Feel free to explore.🔍
      <br>
      <img src={rocket.src} width={rocket.width} height={rocket.height} alt="A rocketship in space." />
		</p>
	</section>
</PageLayout>
